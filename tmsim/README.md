# tmsim - a MiniScript Time Machine Sim

## usage

If mounted to `/usr`, it should autostart when using `reboot`.

If mounted to `/usr2`, run the following to start the game:

```basic
load "/usr2/startup"
run
```
